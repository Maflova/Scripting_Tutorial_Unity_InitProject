// Reference to Namespaces used in the script
using UnityEngine;
using TMPro;

public class DisplayVariable : MonoBehaviour
{

    // Awake is called when a GameObject is first created
    // used to initialize variables and perform other setup tasks
    void Awake()
    {

    }

    // Start is called before the first frame
    // used to initialize the GameObject's state for the start of the game
    void Start()
    {

    }

    // Start is called every frame (60 fps average)
    // used to implement game logic that needs to be updated constantly
    void Update()
    {

    }
}
